version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./cricdata.db:/app/cricdata.db
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped